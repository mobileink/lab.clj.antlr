(ns org.mobileink.clajistics.antlr-test
  (:require [clojure.test :refer :all]
            [org.mobileink.clajistics.antlr :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
